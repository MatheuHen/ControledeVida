import { RoutePlaceholder } from "@/components/route-placeholder";

export default function SharedPage() {
  return (
    <RoutePlaceholder
      title="Compartilhamento"
      description="Fluxo reservado para contextos compartilhados e visualização colaborativa."
    />
  );
}