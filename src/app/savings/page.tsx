import { RoutePlaceholder } from "@/components/route-placeholder";

export default function SavingsPage() {
  return (
    <RoutePlaceholder
      title="Economias"
      description="Base inicial para acompanhar consistência e crescimento de reservas."
    />
  );
}