import { RoutePlaceholder } from "@/components/route-placeholder";

export default function FinancesPage() {
  return (
    <RoutePlaceholder
      title="Finanças"
      description="Área preparada para fluxo de receitas, despesas e saldos."
    />
  );
}